export default function DashboardLayout({children}) {
    return (
        <section>
            <div className="max-w-20 bg-white h-screen w-full p-2">
                
            </div>
            {children}
        </section>
    )
}